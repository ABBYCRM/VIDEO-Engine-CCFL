# Release Naming

The final validated release artifact must be named:

`VIDEO-Engine-Final`

Do not apply the Final label to an unvalidated intermediate build. During implementation use working/checkpoint names; after the Definition of Done is satisfied, package the repository directory and release archive using the Final name.
