"use client";

import { useCallback, useEffect, useState } from "react";
import presets from "@/data/campaign-presets.json";
import backgrounds from "@/data/backgrounds.json";
import tones from "@/data/tones.json";
import avatars from "@/data/avatar-presets.json";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { AppShell } from "@/components/app-shell";
import { AuthGuard } from "@/components/auth-guard";
import { Check, RefreshCcw } from "lucide-react";

type Campaign = { id:string; name:string; category:string; website?:string|null; mission:string; tone?:string|null; platform?:string|null; avatarId?:string|null; backgroundId?:string|null; planningHorizonDays?:number; status:string; createdAt:string };
const HORIZONS=[3,7,14,30] as const;

export default function CampaignsPage(){
  const[name,setName]=useState("CaseClosedFL campaign"),[website,setWebsite]=useState("https://caseclosedfl.com"),[category,setCategory]=useState(presets[0]?.id||"ugc"),[avatarId,setAvatarId]=useState(avatars[0]?.id||""),[tone,setTone]=useState(tones[0]||"direct"),[backgroundId,setBackgroundId]=useState(backgrounds[0]?.id||""),[mission,setMission]=useState("Create a direct-response PI campaign for rideshare passengers who were injured and need a case review."),[planningHorizonDays,setPlanningHorizonDays]=useState<3|7|14|30>(7),[autoPost,setAutoPost]=useState(false),[campaigns,setCampaigns]=useState<Campaign[]>([]),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null),[saved,setSaved]=useState<string|null>(null),[calendarCount,setCalendarCount]=useState(0);
  const load=useCallback(async()=>{const r=await fetch("/api/campaigns",{cache:"no-store"});if(!r.ok)return;const d=await r.json();setCampaigns(d.campaigns||[])},[]);useEffect(()=>{load()},[load]);
  async function createCampaign(){setBusy(true);setError(null);setSaved(null);try{const r=await fetch("/api/campaigns",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,website,category,avatarId,tone,backgroundId,mission,platform:"instagram",planningHorizonDays,autoPost})}),d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);setSaved(d.campaign.id);setCalendarCount(d.calendarCount||0);await load()}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}
  return <AuthGuard><AppShell><main className="mx-auto max-w-7xl px-4 py-8"><div className="mb-6 flex flex-col gap-2"><h1 className="text-3xl font-semibold tracking-tight">Campaigns</h1><p className="max-w-3xl text-slate-600">Create a production campaign and choose how many days the app should pre-fill in Calendar. Each slot starts pending owner approval unless auto-post is explicitly enabled.</p></div>{error&&<div className="mb-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}{saved&&<div className="mb-4 flex items-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700"><Check size={15}/>Campaign saved · {calendarCount} Calendar slot{calendarCount===1?"":"s"} created.</div>}<div className="grid gap-6 xl:grid-cols-[2fr_1fr]"><Card title="Campaign Builder" actions={<Button onClick={createCampaign} disabled={busy||!name.trim()||!mission.trim()}>{busy?<><RefreshCcw size={14} className="mr-2 animate-spin"/>Saving…</>:"Create campaign + fill Calendar"}</Button>}><div className="grid gap-4 md:grid-cols-2"><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Campaign name</span><input value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm"/></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Website</span><input value={website} onChange={e=>setWebsite(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm"/></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Category</span><select value={category} onChange={e=>setCategory(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm">{presets.map(p=><option key={p.id} value={p.id}>{p.label}</option>)}</select></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Avatar</span><select value={avatarId} onChange={e=>setAvatarId(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm">{avatars.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Tone</span><select value={tone} onChange={e=>setTone(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm">{tones.map(t=><option key={t} value={t}>{t}</option>)}</select></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Background</span><select value={backgroundId} onChange={e=>setBackgroundId(e.target.value)} className="w-full rounded-xl border px-4 py-3 text-sm">{backgrounds.map(bg=><option key={bg.id} value={bg.id}>{bg.name}</option>)}</select></label><label className="space-y-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Fill Calendar for</span><select aria-label="Fill Calendar for" value={planningHorizonDays} onChange={e=>setPlanningHorizonDays(Number(e.target.value) as 3|7|14|30)} className="w-full rounded-xl border px-4 py-3 text-sm">{HORIZONS.map(x=><option key={x} value={x}>{x} days</option>)}</select></label><label className="flex items-center gap-3 rounded-xl border p-3"><input type="checkbox" checked={autoPost} onChange={e=>setAutoPost(e.target.checked)}/><span><strong className="text-sm">Auto-post approved Instagram items</strong><br/><span className="text-xs text-slate-500">If off, every Calendar item requires owner approval/manual posting.</span></span></label><label className="space-y-2 md:col-span-2"><span className="text-xs font-semibold uppercase tracking-wide text-slate-500">Mission</span><textarea value={mission} onChange={e=>setMission(e.target.value)} className="min-h-32 w-full rounded-xl border px-4 py-3 text-sm"/></label></div></Card><Card title="Calendar behavior"><ul className="list-disc space-y-2 pl-5 text-sm text-slate-800"><li>3 / 7 / 14 / 30-day horizons are explicit operator choices.</li><li>Generated videos and images attach themselves to Calendar review automatically.</li><li>Owner can approve, edit, post now, or enable auto-post.</li><li>All video engines remain available in Create.</li></ul></Card></div><div className="mt-6"><Card title="Saved campaigns"><div className="grid gap-2">{campaigns.map(c=><div key={c.id} className="rounded-xl border bg-white p-3"><div className="font-medium">{c.name}</div><div className="text-xs text-slate-500">{c.category} · {c.planningHorizonDays||7} days · {c.status} · {new Date(c.createdAt).toLocaleString()}</div><div className="mt-2 line-clamp-2 text-sm text-slate-600">{c.mission}</div></div>)}{!campaigns.length&&<div className="py-6 text-center text-sm text-slate-500">No saved campaigns yet.</div>}</div></Card></div></main></AppShell></AuthGuard>;
}
