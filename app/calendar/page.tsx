"use client";
import {useCallback,useEffect,useMemo,useState} from "react";
import {CalendarDays,ChevronLeft,ChevronRight,Plus,RefreshCcw,Trash2,X} from "lucide-react";
import {AppShell} from "@/components/app-shell";import {AuthGuard} from "@/components/auth-guard";import {Button} from "@/components/ui/button";
const NETWORKS=["instagram","facebook","youtube","tiktok","linkedin"] as const,STATUSES=["draft","pending","approved","published","failed"] as const,FORMATS=["podcast","ugc","newsroom","direct","cinematic"] as const;
type Status=typeof STATUSES[number];type Format=typeof FORMATS[number];type Post={id:string;title:string;network:string;scheduledAt:string;status:Status;autoPost:boolean;caption:string;contentType:Format};
function startOfWeek(input:Date){const d=new Date(input),day=d.getDay();d.setDate(d.getDate()+(day===0?-6:1-day));d.setHours(0,0,0,0);return d}function addDays(d:Date,n:number){const x=new Date(d);x.setDate(x.getDate()+n);return x}function localInputValue(iso?:string){const d=iso?new Date(iso):new Date(Date.now()+3600000),p=(n:number)=>String(n).padStart(2,"0");return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`}
export default function CalendarPage(){const[posts,setPosts]=useState<Post[]>([]),[week,setWeek]=useState(()=>startOfWeek(new Date())),[editing,setEditing]=useState<Post|null|"new">(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);setError(null);try{const r=await fetch("/api/calendar",{cache:"no-store"}),d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);setPosts(d.posts||[])}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);const days=useMemo(()=>Array.from({length:7},(_,i)=>addDays(week,i)),[week]);
async function save(v:{title:string;network:string;scheduledAt:string;status:Status;autoPost:boolean;caption:string;contentType:Format}){const isNew=editing==="new",r=await fetch(isNew?"/api/calendar":`/api/calendar/${(editing as Post).id}`,{method:isNew?"POST":"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({...v,scheduledAt:new Date(v.scheduledAt).toISOString()})}),d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);setEditing(null);await load()}async function remove(p:Post){if(!confirm(`Delete “${p.title}”?`))return;await fetch(`/api/calendar/${p.id}`,{method:"DELETE"});await load()}
return <AuthGuard><AppShell><main><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 flex items-center gap-2 text-sm font-medium text-violet-700"><CalendarDays size={16}/> Publishing workflow</div><h1 className="text-[34px] font-semibold leading-tight tracking-tight text-slate-900">Content Calendar</h1><p className="mt-1 max-w-3xl text-sm text-slate-600">Generated posts land here for owner review. Choose the content format, approve it, or enable auto-post for approved items.</p></div><div className="flex gap-2"><Button variant="secondary" onClick={load} disabled={loading}><RefreshCcw size={14} className="mr-2"/>Refresh</Button><Button onClick={()=>setEditing("new")}><Plus size={14} className="mr-2"/>Add post</Button></div></div>{error&&<div className="mb-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}
<div className="mb-4 grid gap-3 sm:grid-cols-3"><Stat label="Needs approval" value={posts.filter(p=>p.status==="pending").length}/><Stat label="Approved" value={posts.filter(p=>p.status==="approved").length}/><Stat label="Auto-post enabled" value={posts.filter(p=>p.autoPost).length}/></div>
<div className="mb-4 flex items-center justify-between rounded-2xl border border-slate-200 bg-white p-3"><button onClick={()=>setWeek(addDays(week,-7))} aria-label="Previous week"><ChevronLeft/></button><div className="text-center"><div className="text-sm font-semibold">{week.toLocaleDateString(undefined,{month:"long",year:"numeric"})}</div><button className="text-xs text-violet-700" onClick={()=>setWeek(startOfWeek(new Date()))}>Today</button></div><button onClick={()=>setWeek(addDays(week,7))} aria-label="Next week"><ChevronRight/></button></div>
<div className="grid gap-3 lg:grid-cols-7">{days.map(day=>{const items=posts.filter(p=>new Date(p.scheduledAt).toDateString()===day.toDateString());return <section key={day.toISOString()} className="min-h-44 rounded-2xl border border-slate-200 bg-white p-3"><div className="mb-3 flex justify-between"><div><div className="text-[10px] uppercase text-slate-500">{day.toLocaleDateString(undefined,{weekday:"short"})}</div><div className="text-xl font-semibold">{day.getDate()}</div></div><button onClick={()=>setEditing("new")} aria-label={`Add post ${day.toDateString()}`}><Plus size={14}/></button></div><div className="grid gap-2">{items.map(p=><button key={p.id} onClick={()=>setEditing(p)} className="rounded-xl border border-slate-200 bg-slate-50 p-2 text-left hover:border-violet-300"><div className="text-xs font-semibold">{p.title}</div><div className="mt-1 text-[10px] uppercase text-violet-700">{p.contentType} · {p.network}</div><div className="text-[10px] text-slate-500">{p.status}{p.autoPost?" · auto":""}</div></button>)}</div></section>})}</div>
<div className="mt-6 rounded-2xl border border-slate-200 bg-white p-4"><div className="mb-3 font-semibold">Approval queue</div><div className="grid gap-2">{posts.filter(p=>p.status==="pending"||p.status==="approved").map(p=><div key={p.id} className="flex items-center gap-3 rounded-xl border p-3"><button className="min-w-0 flex-1 text-left" onClick={()=>setEditing(p)}><div className="font-medium">{p.title}</div><div className="text-xs text-slate-500">{p.contentType} · {p.network} · {new Date(p.scheduledAt).toLocaleString()}</div></button><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-semibold uppercase">{p.status}</span><button onClick={()=>remove(p)} aria-label={`Delete ${p.title}`}><Trash2 size={14}/></button></div>)}</div></div></main>{editing&&<PostModal post={editing==="new"?null:editing} onClose={()=>setEditing(null)} onSave={save}/>}</AppShell></AuthGuard>}
function Stat({label,value}:{label:string;value:number}){return <div className="rounded-2xl border border-slate-200 bg-white p-4"><div className="text-2xl font-semibold">{value}</div><div className="text-xs text-slate-500">{label}</div></div>}
function PostModal({post,onClose,onSave}:{post:Post|null;onClose:()=>void;onSave:(v:{title:string;network:string;scheduledAt:string;status:Status;autoPost:boolean;caption:string;contentType:Format})=>Promise<void>}){const[title,setTitle]=useState(post?.title||""),[network,setNetwork]=useState(post?.network||"instagram"),[scheduledAt,setScheduledAt]=useState(localInputValue(post?.scheduledAt)),[status,setStatus]=useState<Status>(post?.status||"pending"),[contentType,setContentType]=useState<Format>(post?.contentType||"podcast"),[autoPost,setAutoPost]=useState(Boolean(post?.autoPost)),[caption,setCaption]=useState(post?.caption||""),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(null);try{await onSave({title,network,scheduledAt,status,autoPost,caption,contentType})}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/35 p-4"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl bg-white p-5 shadow-xl"><div className="mb-4 flex justify-between"><strong>{post?"Review scheduled post":"Schedule content"}</strong><button type="button" onClick={onClose} aria-label="Close"><X/></button></div><div className="grid gap-3"><label>Title<input required value={title} onChange={e=>setTitle(e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><div className="grid grid-cols-2 gap-3"><label>Format<select value={contentType} onChange={e=>setContentType(e.target.value as Format)} className="mt-1 h-11 w-full rounded-xl border px-3">{FORMATS.map(x=><option key={x}>{x}</option>)}</select></label><label>Network<select value={network} onChange={e=>setNetwork(e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3">{NETWORKS.map(x=><option key={x}>{x}</option>)}</select></label></div><label>Status<select value={status} onChange={e=>setStatus(e.target.value as Status)} className="mt-1 h-11 w-full rounded-xl border px-3">{STATUSES.map(x=><option key={x}>{x}</option>)}</select></label><label>Date & time<input type="datetime-local" required value={scheduledAt} onChange={e=>setScheduledAt(e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Caption / notes<textarea rows={4} value={caption} onChange={e=>setCaption(e.target.value)} className="mt-1 w-full rounded-xl border p-3"/></label><label className="flex gap-3 rounded-xl border p-3"><input type="checkbox" checked={autoPost} onChange={e=>setAutoPost(e.target.checked)}/><span><strong>Auto-post after approval</strong><br/><span className="text-xs text-slate-500">Uses the connected Composio publishing account.</span></span></label>{error&&<div className="text-sm text-rose-700">{error}</div>}</div><div className="mt-5 flex justify-end gap-2"><Button type="button" variant="secondary" onClick={onClose}>Cancel</Button><Button type="submit" disabled={busy||!title.trim()}>{busy?"Saving…":"Save"}</Button></div></form></div>}
