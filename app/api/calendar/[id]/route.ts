import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/auth";
import { db } from "@/lib/db";
const NETWORKS=new Set(["instagram","facebook","youtube","tiktok","linkedin"]),STATUSES=new Set(["draft","pending","approved","published","failed"]),FORMATS=new Set(["podcast","ugc","newsroom","direct","cinematic"]);
function rowToPost(row:any){return{id:row.id,title:row.title,network:row.network,scheduledAt:row.scheduled_at,status:row.status,autoPost:Boolean(row.auto_post),caption:row.caption,contentType:row.content_type||"ugc",videoJobId:row.video_job_id,connectedAccountId:row.connected_account_id,publishedAt:row.published_at,error:row.error,createdAt:row.created_at,updatedAt:row.updated_at};}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!(await requireAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params,current=db.prepare("SELECT * FROM scheduled_posts WHERE id=?").get(id) as any;if(!current)return NextResponse.json({error:"Not found"},{status:404});const body=await req.json().catch(()=>({})),title=body.title===undefined?current.title:String(body.title).trim().slice(0,180),network=body.network===undefined?current.network:String(body.network).toLowerCase(),status=body.status===undefined?current.status:String(body.status),contentType=body.contentType===undefined?(current.content_type||"ugc"):String(body.contentType).toLowerCase();let scheduledAt=current.scheduled_at;if(body.scheduledAt!==undefined){const d=new Date(String(body.scheduledAt));if(Number.isNaN(d.getTime()))return NextResponse.json({error:"Invalid scheduledAt"},{status:400});scheduledAt=d.toISOString();}if(!title)return NextResponse.json({error:"Title is required"},{status:400});if(!NETWORKS.has(network)||!STATUSES.has(status)||!FORMATS.has(contentType))return NextResponse.json({error:"Invalid schedule fields"},{status:400});db.prepare(`UPDATE scheduled_posts SET title=?,network=?,scheduled_at=?,status=?,auto_post=?,caption=?,content_type=?,connected_account_id=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).run(title,network,scheduledAt,status,body.autoPost===undefined?current.auto_post:body.autoPost?1:0,body.caption===undefined?current.caption:String(body.caption).slice(0,5000),contentType,body.connectedAccountId===undefined?current.connected_account_id:(body.connectedAccountId?String(body.connectedAccountId):null),id);return NextResponse.json({post:rowToPost(db.prepare("SELECT * FROM scheduled_posts WHERE id=?").get(id))});}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){if(!(await requireAdmin()))return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params,result=db.prepare("DELETE FROM scheduled_posts WHERE id=?").run(id);if(!result.changes)return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json({ok:true});}
