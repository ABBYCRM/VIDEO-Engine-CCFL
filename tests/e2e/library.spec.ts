import { expect,test } from "@playwright/test";
import { stubAuthenticatedSession } from "./helpers";

test("Library filters and downloads generated images and videos",async({page})=>{await stubAuthenticatedSession(page);await page.route("**/api/library",route=>route.fulfill({status:200,contentType:"application/json",body:JSON.stringify({assets:[{id:"generated:1",kind:"generated",mediaType:"image",label:"Generated avatar",title:"FLUX portrait",url:"/test-avatar.png",model:"black-forest-labs/flux.1-schnell",prompt:"internal",createdAt:new Date().toISOString()},{id:"video:1",kind:"video",mediaType:"video",label:"ugc video",title:"HEDRA generated video",url:"/test-video.mp4",model:"hedra-character-3",prompt:null,createdAt:new Date().toISOString()},{id:"avatar:female:front",kind:"turnaround",mediaType:"image",label:"front view",title:"Female Anchor 01",url:"/front.png",model:"gemini-2.5-flash-image",prompt:null,createdAt:new Date().toISOString()}]})}));await page.route("**/test-avatar.png",route=>route.fulfill({status:200,contentType:"image/png",body:Buffer.from("89504e470d0a1a0a","hex")}));await page.route("**/front.png",route=>route.fulfill({status:200,contentType:"image/png",body:Buffer.from("89504e470d0a1a0a","hex")}));await page.route("**/test-video.mp4",route=>route.fulfill({status:200,contentType:"video/mp4",body:"fake"}));await page.goto("/library");await expect(page.getByRole("heading",{name:"Library"})).toBeVisible();await expect(page.getByText("FLUX portrait")).toBeVisible();await expect(page.getByText("HEDRA generated video")).toBeVisible();await expect(page.getByText("Female Anchor 01")).toBeVisible();await page.getByRole("button",{name:"videos",exact:true}).click();await expect(page.getByText("HEDRA generated video")).toBeVisible();await expect(page.getByText("FLUX portrait")).toHaveCount(0);let download=page.waitForEvent("download");await page.getByRole("button",{name:"Download"}).click();let file=await download;expect(file.suggestedFilename()).toMatch(/hedra-generated-video-ugc-video\.mp4$/);await page.getByRole("button",{name:"images",exact:true}).click();await expect(page.getByText("FLUX portrait")).toBeVisible();download=page.waitForEvent("download");await page.getByRole("button",{name:"Download"}).first().click();file=await download;expect(file.suggestedFilename()).toMatch(/\.png$/);await page.getByLabel("Search library").fill("Female Anchor");await expect(page.getByText("Female Anchor 01")).toBeVisible();});
