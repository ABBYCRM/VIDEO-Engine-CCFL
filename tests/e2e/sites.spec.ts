import { expect, test } from "@playwright/test";
import { stubAuthenticatedSession } from "./helpers";

test("Sites supports blog-only onboarding header code and calendar planning",async({page})=>{await stubAuthenticatedSession(page);let sites:any[]=[];let planned:any=null;await page.route("**/api/sites",async route=>{const req=route.request();if(req.method()==="GET")return route.fulfill({status:200,contentType:"application/json",body:JSON.stringify({sites})});if(req.method()==="POST"){const input=await req.postDataJSON();const site={id:"site-1",...input,url:"https://example.com",verifiedAt:null,lastSeenAt:null,status:"setup",bridgeToken:"ve_site_test"};sites=[site];return route.fulfill({status:201,contentType:"application/json",body:JSON.stringify({site})});}return route.continue();});await page.route("**/api/sites/site-1",route=>route.fulfill({status:200,contentType:"application/json",body:JSON.stringify({site:{...sites[0],bridgeToken:"ve_site_test"}})}));await page.route("**/api/sites/site-1/plan",async route=>{planned=await route.request().postDataJSON();return route.fulfill({status:201,contentType:"application/json",body:JSON.stringify({ok:true,count:14})});});await page.goto("/sites");await expect(page.getByRole("heading",{name:"Sites"})).toBeVisible();await expect(page.getByText(/blog \+ images only/i)).toBeVisible();await page.getByRole("button",{name:"Add site"}).click();await page.getByLabel("Site name").fill("Example Blog");await page.getByLabel("Website URL").fill("https://example.com");await page.getByLabel("Image style").selectOption("hyper-realistic");await expect(page.getByText(/No video settings exist in this workflow/i)).toBeVisible();await page.getByRole("button",{name:"Save site"}).click();await expect(page.getByText("Install header code")).toBeVisible();await expect(page.locator("pre")).toContainText("/api/sites/bridge.js?key=ve_site_test");await page.getByRole("button",{name:"Close",exact:true}).last().click();await page.getByLabel("Planning horizon for Example Blog").selectOption("14");await page.getByRole("button",{name:"Generate blog calendar"}).click();await expect(page.getByText(/14 NVIDIA blog drafts added to Calendar/)).toBeVisible();expect(planned.horizonDays).toBe(14);await expect(page.getByRole("button",{name:"Generate blog calendar"})).toBeVisible();});
